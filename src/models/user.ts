export type typeUser = {
    congregation: number;
    email: string
    // group: number
    hthAssignments: number[]
    id: number
    isActive: boolean
    isAdmin: boolean
    isAuth: boolean
    isBuildingManager: boolean
    phoneAssignments: number[]
    role: number
}
